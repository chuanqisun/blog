<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="An Inquiry into Zeros and Ones">
  <meta name="author" content="Chuanqi Sun">
	<title>{{ title }}</title>
	<link rel="stylesheet" href="/style.css">
</head>
<body>
	{% include "partials/nav.njk" %}
	<header>
		<h1>{{ title }}</h1>
		<div><time datetime="{{date | machineDate}}">{{ date | humanDate }}<time></div> <div>{% for tag in keywords %} #{{ tag }} {% endfor %}</div>
	</header>
	<main>
		{{ content | safe }}
	</main>
</body>
</html>